import Sidebar from './sidebar'


const Profile = () => {
    return (
        <div style={{
            display: "flex",
            flexDirection: "row"
          }}>
            <Sidebar />
            {/* Need to design */}
            Profile page
        </div>
    );
}

export default Profile;