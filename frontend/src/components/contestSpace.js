import Sidebar from './sidebar'


const Contest = () => {
    return (
        <div style={{
            display: "flex",
            flexDirection: "row"
          }}>
            <Sidebar />
            {/* Need to design */}
            Contest page
        </div>
    );
}

export default Contest;